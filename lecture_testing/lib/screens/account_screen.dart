import 'package:flutter/material.dart';

class AccountScreenWidget extends StatefulWidget {
  const AccountScreenWidget({super.key});

  @override
  State<AccountScreenWidget> createState() => _AccountScreenWidgetState();
}

class _AccountScreenWidgetState extends State<AccountScreenWidget> {
  @override
  Widget build(BuildContext context) {
    return const Text("HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING HEADACHING");
  }
}